# دليل الاستخدام والتشغيل والربط الكامل للمشروع (NileTechno E-Commerce)

تم تصميم وتطوير هذا المشروع بجودة عالية، مع تطبيق أحدث معايير تجربة المستخدم وألوان وتفاصيل التصميم التي أرسلتها في الصور تماماً. يوضح هذا الملف طريقة عمل الكود بالتفصيل، وكيفية ربطه بالسيرفر والـ API، وتعديله ليتناسب مع أي قاعدة بيانات أخرى بمرونة تامة.

---

## 1. هيكلية الملفات ووظيفة كل ملف كود في التطبيق

التطبيق مبني بهيكل معياري منظم للغاية، حيث قمنا مؤخراً بتقسيم الكود إلى مجلدات مخصصة داخل مجلد المكونات `/src/components/` ليسهل عليك تصفحها والوصول إلى ملف الكود الخاص بكل صفحة وميزة بشكل مستقل وبسيط:

*   **`/src/App.jsx`**: المحرك والمنسق الأساسي لصفحات الموقع، حيث يقوم بالتحكم في الحالات العامة والتنقل بين الصفحات والتبويبات.
*   **`/src/components/Home/` (قسم الصفحة الرئيسية)**:
    *   `HomeTab.jsx`: المكون الرئيسي للتبويب الأول، ويعرض واجهة المتجر الرئيسية والبحث السريع والراية الإعلانية وشبكة المنتجات.
    *   `FeaturedBanner.jsx`: بنر الترويج الخاص بالصفحة الرئيسية لـ "منتج اليوم".
    *   `QuickLinks.jsx`: أزرار الوصول السريع والفلترة بالأعلى (عروض، جديد، مميز).
    *   `CategoryBar.jsx`: شريط التصفح السريع للأقسام بأيقوناتها الأنيقة.
    *   `SmartFilters.jsx`: لوحة الفلترة والترتيب الذكي حسب السعر والمخزون.
    *   `ProductCard.jsx`: كرت عرض المنتج الذكي والمميز.
*   **`/src/components/Categories/` (قسم تصفح الأقسام)**:
    *   `CategoriesTab.jsx`: يعرض كافة الأقسام المتوفرة في المتجر على شكل كروت كحلية فخمة مع عدد المنتجات المتاحة في كل فئة والتحول المباشر إليها.
*   **`/src/components/Search/` (قسم محرك البحث الشامل)**:
    *   `SearchTab.jsx`: محرك البحث المتطور والذكي الذي يبحث في الأسماء والمواصفات والتصنيفات بدقة فائقة.
*   **`/src/components/Wishlist/` (قسم المفضلة)**:
    *   `WishlistTab.jsx`: واجهة عرض المنتجات التي نالت إعجاب المستخدم وقام بحفظها للرجوع إليها لاحقاً.
*   **`/src/components/ProductDetails/` (قسم تفاصيل المنتج)**:
    *   `ProductDetailsModal.jsx`: صفحة تفاصيل المنتج التفاعلية (تقييم، خيارات الألوان والسعة، صور مصغرة تفاعلية، والمواصفات).
*   **`/src/components/Cart/` (قسم سلة المشتريات)**:
    *   `CartDrawer.jsx`: الدرج الجانبي الانزلاقي الفاخر لإدارة محتويات السلة، تطبيق كوبونات الخصم، ومجموع الحساب بالتفصيل.
*   **`/src/components/Checkout/` (قسم إتمام الدفع والطلب)**:
    *   `CheckoutModal.jsx`: المنسق الأساسي لعملية الشراء والدفع.
    *   `CheckoutAddressStep.jsx` & `CheckoutPaymentStep.jsx` & `CheckoutReviewStep.jsx`: خطوات إدخال العنوان والموقع على الخريطة، اختيار طريقة الدفع ورفع الإيصال، ومراجعة الفاتورة النهائية.
    *   `InteractiveMap.jsx`: خريطة تفاعلية بالكامل لتحديد موقع توصيل الطلب للعميل بدقة وسهولة.
*   **`/src/components/Account/` (قسم الحساب والمستخدم)**:
    *   `MyAccount.jsx`: صفحة "حسابي" الشخصية (النقاط، إحصائيات، تتبع الطلبات، إعدادات المتجر المتطورة والربط بالـ API الخارجي، وواجهة لوحة الإدارة للمشرف).
    *   `LoginForm.jsx`: واجهة تسجيل الدخول والاشتراك وتجربة الزائر.
*   **`/src/components/Layout/` (العناصر الهيكلية المشتركة)**:
    *   `Navbar.jsx`: شريط التنقل العلوي الذكي مع تبويبات الهواتف المحمولة وتنبيهات السلة والمفضلة.
    *   `Footer.jsx`: الفوتر البسيط المتناسق بذات تيم الموقع.
    *   `Logo.jsx`: المكون المسؤول عن رسم شعار NileTechno الفاخر.
*   **`/src/api.js`**: صلة الوصل بين جميع هذه الواجهات وسيرفرات الـ API أو التخزين المحلي.

---

## 2. ميزة الربط التلقائي واليدوي بأي API خارجي (بدون تعديل كود)

استجابة لرغبتك في إلغاء قيود قاعدة البيانات الصارمة وجعل التطبيق قادراً على الاتصال بأي سيرفر API خارجي تلقائياً أو يدوياً، قمنا بابتكار وتطبيق الآتي:

1.  **الربط التلقائي (الافتراضي)**:
    *   يرتبط الفرونت إند تلقائياً بمسارات نسبية مثل `/api/products` و `/api/orders` والتي تخدمها خوادم Express المتواجدة في المشروع محلياً، مما يضمن عمل التطبيق بنجاح كامل فور التشغيل مباشرة.
2.  **الربط اليدوي المرن بالـ API الخارجي**:
    *   تم تعديل كرت الإرسال في ملف `/src/api.js` ليقوم بفحص حقل `custom_api_url` المحفوظ في الذاكرة المحلية للمتصفح (localStorage) قبل إرسال أي طلب.
    *   إذا قمت بوضع رابط السيرفر الخاص بك (مثلاً: `https://my-backend-server.com`) في صفحة **"إعدادات الـ API والاتصال"** المتواجدة داخل قسم **"إعدادات التطبيق"** بصفحة **"حسابي"**، فسيقوم التطبيق تلقائياً وبشكل فوري بتحويل كل الطلبات وجلب المنتجات وعرض الأقسام وإرسال الفواتير إلى السيرفر الجديد الخاص بك دون أن تضطر لتغيير سطر كود واحد!

---

## 3. تشغيل المشروع على سيرفر خارجي أو على جهازك محلياً (Localhost)

لتشغيل البروجيكت بالكامل على جهازك أو رفعه على سيرفر خاص، تتبع الخطوات البسيطة التالية:

### المتطلبات الأساسية:
*   تثبيت برنامج **Node.js** (الإصدار 18 أو أحدث) على جهازك أو سيرفر الاستضافة.

### خطوات التشغيل المحلي (على جهازك):
1.  قم بفك ضغط ملف المشروع وافتح منفذ الأوامر (Terminal) في المجلد الرئيسي للمشروع.
2.  قم بتثبيت الحزم البرمجية والاعتماديات عن طريق تشغيل الأمر التالي:
    ```bash
    npm install
    ```
3.  لتشغيل التطبيق في وضع التطوير البرمجي (Development Mode) مع السيرفر المحلي والفرونت إند معاً، قم بتشغيل الأمر:
    ```bash
    npm run dev
    ```
    *سيظهر لك رابط تشغيل محلي مثل `http://localhost:3000` يمنحك معاينة فورية فائقة السرعة للتطبيق.*

### خطوات التشغيل على سيرفر استضافة خارجي (Production):
1.  قم ببناء الملفات البرمجية لتجهيزها للإنتاج بأعلى سرعة وكفاءة بطلب الأمر التالي:
    ```bash
    npm run build
    ```
2.  قم بتشغيل السيرفر الجاهز والمبني عن طريق تشغيل الأمر:
    ```bash
    npm run start
    ```
    *سيتم بدء تشغيل الخادم والاتصال بالمنفذ 3000 بنجاح تام.*

---

## 4. كيفية تكييف التطبيق في حال قمت بتغيير أسماء أعمدة أو حقول قاعدة البيانات

إذا قررت استخدام الـ API الخاص بك وكانت أسماء الأعمدة في قاعدة بياناتك تختلف عن الأسماء الحالية (مثل استخدام `product_id` بدلاً من `id` أو `product_title` بدلاً من `name` أو `sale_price` بدلاً من `price`)، فلا تقلق! يمكنك حل هذا التعديل في مكان واحد فقط وبسهولة متناهية دون كسر واجهات التطبيق:

### طريقة عمل مواءمة البيانات (Data Mapping):
افتح الملف `/src/api.js` وقم بتعديل دالة `apiRequest` لتعديل مخرجات السيرفر وجعلها تطابق الأسماء التي يفهمها الفرونت إند، على النحو التالي:

```javascript
// مثال لتعديل البيانات القادمة من السيرفر لتوافق واجهات التطبيق تلقائياً
async function apiRequest(url, method = "GET", body) {
  // الكود الأساسي للإرسال...
  const response = await fetch(finalUrl, options);
  const data = await response.json();

  // مواءمة بيانات المنتجات القادمة من السيرفر الخاص بك:
  if (url === "/api/products" && method === "GET") {
    return data.map(item => ({
      id: item.product_id,              // مواءمة المعرف المخصص
      name: item.product_title,          // مواءمة عنوان المنتج
      description: item.long_desc,       // مواءمة الوصف المخصص
      price: item.sale_price,            // مواءمة سعر البيع المخصص
      originalPrice: item.regular_price, // مواءمة السعر الأصلي المخصص
      stock: item.inventory_quantity,    // مواءمة الكمية المتوفرة
      category: item.category_slug,      // مواءمة اسم التصنيف
      image: item.main_image_url,        // مواءمة الصورة الأساسية
      gallery_urls: item.media_gallery   // مواءمة معرض الصور
    }));
  }

  return data;
}
```

باستخدام هذه التقنية العبقرية (Data Mapping)، تضمن بقاء كود الواجهات البرمجية نظيفاً وثابتاً تماماً، بينما تقوم فقط بضبط أسماء الحقول ومواءمتها مع الـ API الجديد داخل دالة الاتصال الموحدة بملف الـ API!
