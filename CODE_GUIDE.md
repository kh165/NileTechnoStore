# دليل المطور وفهم بنية الكود لـ معرض NileTechno 🚀

مرحباً بك! لقد تم تصميم هذا الدليل باللغة العربية البسيطة لتفهم كل جزء في موقعك بالتفصيل، وتعرف أين توجد الـ APIs وكيف تعمل، وكيف يمكنك إدارتها أو ربطها مستقبلاً بقاعدة بيانات حقيقية بسهولة تامة.

---

## 📂 أولاً: هيكلية المجلدات والملفات (Directory Structure)

يتكون المشروع من الهيكل المنظم التالي:

```text
├── package.json               # يحتوي على المكتبات والتبعيات المثبتة والأوامر البرمجية للتشغيل والبناء.
├── index.html                 # الملف الأساسي الذي يحتوي على نقطة الدخول لمتصفح الويب.
├── src/
│   ├── main.tsx               # نقطة البدء للغة الـ TypeScript لتشغيل تطبيق React.
│   ├── index.css              # ملف التصميم الرئيسي الذي يستورد مكتبة Tailwind CSS ويحدد الألوان والخطوط.
│   ├── App.jsx                # منسق التطبيق والصفحة الرئيسية (المنظم لحالة السلة، البحث، الانتقال بين الصفحات).
│   ├── api.js                 # قاعدة البيانات المحلية والاتصالات بالـ API.
│   └── components/            # مجلد المكونات المصنف ومقسم إلى مجلدات فرعية لكل صفحة:
│       ├── Home/              # قسم الصفحة الرئيسية وعناصرها
│       │   ├── HomeTab.jsx
│       │   ├── FeaturedBanner.jsx
│       │   ├── QuickLinks.jsx
│       │   ├── CategoryBar.jsx
│       │   ├── SmartFilters.jsx
│       │   └── ProductCard.jsx
│       ├── Categories/        # قسم تبويب الأقسام والكروت الخاصة بها
│       │   └── CategoriesTab.jsx
│       ├── Search/            # قسم تبويب محرك البحث الذكي
│       │   └── SearchTab.jsx
│       ├── Wishlist/          # قسم تبويب المفضلة وقائمتها
│       │   └── WishlistTab.jsx
│       ├── ProductDetails/    # قسم صفحة تفاصيل المنتج المنبثقة
│       │   └── ProductDetailsModal.jsx
│       ├── Cart/              # قسم سلة المشتريات والتحكم بها
│       │   └── CartDrawer.jsx
│       ├── Checkout/          # قسم خطوات الدفع والشحن والخريطة التفاعلية
│       │   ├── CheckoutModal.jsx
│       │   ├── CheckoutAddressStep.jsx
│       │   ├── CheckoutPaymentStep.jsx
│       │   ├── CheckoutReviewStep.jsx
│       │   └── InteractiveMap.jsx
│       ├── Account/           # قسم الحساب الشخصي وتسجيل الدخول للمشرف والعميل
│       │   ├── MyAccount.jsx
│       │   └── LoginForm.jsx
│       └── Layout/            # العناصر الهيكلية المشتركة للموقع
│           ├── Navbar.jsx
│           ├── Footer.jsx
│           └── Logo.jsx
```

---

## ⚙️ ثانياً: شرح كود الـ API ومحاكاة البيانات (`src/api.js`)

ملف `src/api.js` هو **المحرك الرئيسي والقلب المغذي للموقع** ببيانات المنتجات، الطلبات، الإعدادات، وطرق الدفع والشحن.

### 📍 أين يوجد ملف الـ API؟
يوجد في المسار المباشر التالي: `/src/api.js`

### ❓ لماذا صُمم بهذا الشكل؟
لقد تم إلغاء أي اتصال بسيرفرات خارجية أو قواعد بيانات سحابية لجعل الموقع **يعمل بنسبة 100% محلياً وآمناً وسريعاً بشكل مذهل (Offline-First)** باستخدام الـ `localStorage` في متصفح العميل. 
هذا يعني أن:
- بيانات المنتجات والتصنيفات تُحفظ وتُعرض تلقائياً.
- أي عملية شراء جديدة أو تعديل على المنتجات يُخزن بشكل دائم في متصفحك حتى لو قمت بإعادة تشغيل الصفحة.

---

### 🛠️ تفصيل الدوال الموجودة داخل `src/api.js` وما وظيفتها ومتى تحتاجها:

#### 1️⃣ دوال إدارة المنتجات (Products API)
- `getProducts()`:
  - **ماذا تفعل؟**: تجلب قائمة بجميع المنتجات وتدمج معها اسم التصنيف التابع لها.
  - **متى تحتاجها؟**: عند عرض المنتجات في الصفحة الرئيسية أو البحث عنها.
- `saveProduct(product)`:
  - **ماذا تفعل؟**: تضيف منتجاً جديداً وتحدد معايير السعر والخصم والصورة وحفظها.
  - **متى تحتاجها؟**: في لوحة تحكم الإدارة (الخاصة بالمشرف) لإدراج منتج جديد بالمخزن.
- `updateProduct(id, product)`:
  - **ماذا تفعل؟**: تعديل بيانات منتج موجود مسبقاً (الاسم، السعر، الصورة، الكمية).
  - **متى تحتاجها؟**: عند رغبتك في تحديث مخزون أو مواصفات منتج معين.
- `deleteProduct(id)`:
  - **ماذا تفعل؟**: تحذف المنتج نهائياً من قاعدة البيانات المحلية.
  - **متى تحتاجها؟**: للتخلص من المنتجات التي لم تعد متوفرة.

#### 2️⃣ دوال إدارة الطلبات (Orders API)
- `getOrders()`:
  - **ماذا تفعل؟**: استرجاع جميع الطلبات التي قام العملاء بتسجيلها.
  - **متى تحتاجها؟**: في لوحة المشرف لمتابعة الطلبات وتجهيزها، وفي صفحة حساب العميل لمشاهدة سجل مشترياته.
- `placeOrder(order)`:
  - **ماذا تفعل؟**: تحويل السلة الحالية إلى طلب رسمي يحمل رقم طلب فريد (مثل `ORD-583012`) وحساب المجموع والضرائب والشحن ونوعه وتخزينه.
  - **متى تحتاجها؟**: عند ضغط العميل على "تأكيد الطلب" في نهاية خطوات الدفع.
- `updateOrderStatus(id, status)`:
  - **ماذا تفعل؟**: لتحديث حالة الطلب (مثال: قيد الانتظار 🡨 جاري التجهيز 🡨 تم الشحن 🡨 تم التسليم).
  - **متى تحتاجها؟**: لإعلام العميل بتحديثات شحن طلبيته.

#### 3️⃣ دوال إدارة وتخصيص الموقع (Settings, Shipping & Payment)
- `getCategories()` / `addCategory(name)`: للحصول على تصنيفات المنتجات (مثل: منتجات رقمية، إلكترونيات) وإضافة تصنيفات جديدة.
- `getPaymentMethods()`: للحصول على طرق الدفع المتاحة بالموقع (كاش، فيزا، فودافون كاش، انستا باي).
- `getShippingMethods()`: جلب مناطق وتكاليف الشحن المحددة (القاهرة والجيزة، المحافظات، إلخ).
- `getAnalytics()` / `getStats()`: حساب ذكي وفوري لإجمالي الأرباح، نسبة الطلبات المعلقة، وعدد المنتجات لتقديم لوحة إحصائيات بصرية متكاملة للمشرف.
- `getAdminConfig()` / `updateAdminConfig(payload)`: قراءة وتحديث معلومات المتجر الأساسية وكلمة مرور لوحة الإدارة (كلمة المرور الافتراضية هي: `admin`).

---

## 💡 كيف تستفيد من هذا الملف في المستقبل لربطه بسيرفر حقيقي؟

إذا قررت لاحقاً رفع هذا الموقع على سيرفر حقيقي خاص بك وربطه بقاعدة بيانات سحابية (مثل MongoDB أو SQL أو Firebase)، **لن تحتاج على الإطلاق لتعديل أي كود في الواجهات الرسومية لـ React!**
كل ما عليك فعله هو الدخول إلى ملف `src/api.js` وتحديث الدوال داخله لتقوم بعمل جلب (fetch) حقيقي من السيرفر الخاص بك بدلاً من الـ `localStorage`. 

**مثال للتوضيح:**
بدلاً من جلب المنتجات محلياً:
```javascript
getProducts: () => {
  return Promise.resolve(getLocalData("products"));
}
```

ستقوم بكتابتها هكذا لترتبط بقاعدتك الحقيقية مباشرة:
```javascript
getProducts: async () => {
  const response = await fetch("https://your-api-domain.com/api/products");
  return response.json();
}
```

---

## 📱 ثالثاً: تفاصيل التحسينات المطبقة على السلة والواجهات

1. **تصميم سلة المشتريات الجديد (`src/components/CartDrawer.jsx`)**:
   - تم تحويل السلة تماماً لتطابق الصورة المطلوبة بلمسات برمجية عصرية.
   - على شاشات الهواتف المحمولة، تظهر السلة بشكل كامل وتأخذ كامل الشاشة لتسهيل التصفح والتفاعل مع الأزرار دون حدوث أي تداخل في الحجم.
   - تمت إضافة **زر خروج/رجوع فوري وبارز جداً على شكل دائرة مع سهم لليمين (`→`) في الزاوية العلوية**، بمجرد النقر عليه يتم غلق السلة والعودة للمتجر بسلاسة.
   - ترتيب المنتجات داخل الكارت: تظهر صورة المنتج على اليسار (أو اليمين بما يتوافق مع RTL العربي)، وبجانبها التفاصيل (الاسم، التصنيف باللون الأزرق، السعر باللون الأزرق المميز)، وعلى الطرف الأخر يتواجد زر الحذف وسفاري التحكم بالكمية (`+ 1 -`) داخل كبسولة رمادية فخمة وناعمة جداً.
   - كوبون الخصم التفاعلي: يمكنك تجربة كتابة الكوبونات الحقيقية مثل **`KHALID`** أو **`NILE2026`** لتطبيق خصم مباشر وفوري 10% أو **`SALE20`** لخصم 20% ومشاهدة تفاصيل الحسابات تتغير وتتحدث في ثوانٍ معدودة.

2. **التجاوب المطلق مع جميع أحجام الشاشات (Responsive Design)**:
   - تم فحص وتعديل جميع المكونات والعناصر البرمجية في المتجر لتتطابق وتقدم تجربة تصفح مثالية على:
     - الهواتف المحمولة الصغيرة جداً.
     - الأجهزة اللوحية (Tablets).
     - شاشات الحواسب العادية والعملاقة.
   - استخدام مرونة الـ Flexbox والتخطيط الشبكي لـ Tailwind CSS في توزيع العناصر والمربعات لتتناسب مع التمرير العمودي وتمنع أي خروج أو كسر في مظهر الموقع.

3. **تبديل بليس هولدر التسجيل**:
   - تم تعديل البليس هولدر في حقول إدخال الاسم بالكامل عند إنشاء حساب جديد ليكون اسم العميل والضيف الرائع **"خالد صلاح"** بدلاً من الاسم القديم.

---

لقد تم بناء وبرمجة هذا المتجر ليكون منارة للتسوق الذكي والأداء البصري العالي. تصفح بكل ثقة وسعادة! 🛍️✨
